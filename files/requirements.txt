# CATATAN: jangan pasang dengan `pip install -r requirements.txt` saja.
# openwakeword harus dipasang terpisah dengan --no-deps. Pakai ./install.sh
#
# Alasannya: metadata openwakeword mewajibkan tflite-runtime di Linux, dan
# tflite-runtime tidak merilis wheel untuk Python 3.12 ke atas. Kita pakai
# backend ONNX-nya, jadi tflite memang tidak dibutuhkan.

sounddevice>=0.4.6
numpy>=1.24
faster-whisper>=1.0.0
piper-tts>=1.3.0
google-genai>=2.0.0

# Dependensi asli openwakeword, minus tflite-runtime.
onnxruntime>=1.10.0,<2
tqdm>=4.0,<5
scipy>=1.3,<2
scikit-learn>=1,<2
requests>=2.0,<3
