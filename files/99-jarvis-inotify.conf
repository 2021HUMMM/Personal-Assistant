# Menaikkan limit inotify — dipakai systemd buat mengawasi cgroup tiap service
# (termasuk jarvis.service) dan oleh banyak app (VS Code, dst). Default 128
# instance gampang habis di mesin dengan banyak service user berjalan.
fs.inotify.max_user_instances = 512
fs.inotify.max_user_watches = 524288
